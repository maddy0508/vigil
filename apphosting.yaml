# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
# env is a list of environment variables.
# You can also use secrets from Google Secret Manager.
# https://firebase.google.com/docs/app-hosting/configure#set-environment-variables
env:
  - variable: GEMINI_API_KEY
    # You can create the secret by running this command in your terminal:
    # gcloud secrets create "GEMINI_API_KEY" --project="<your-project-id>"
    # See https://firebase.google.com/docs/app-hosting/manage-secrets for more info.
    secret: GEMINI_API_KEY
